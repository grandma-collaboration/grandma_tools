# Skyportal API Configuration
SKYPORTAL_API_TOKEN=your_skyportal_api_token_here
SKYPORTAL_BASE_URL=https://fritz.science/api

# Group IDs to fetch sources from (comma-separated)
GROUP_IDS=1840

# Time window filter for when sources were saved to the group
# Format: YYYY-MM-DDTHH:MM:SS (ISO 8601 format)
# Leave empty to disable filtering
SAVED_AFTER=2025-11-07T13:00:00
SAVED_BEFORE=2025-12-04T13:00:00

# Source filters (optional)
# Filter by classification type (comma-separated, OR logic - matches if ANY classification is present)
# Example: Ia,Supernova,Ic-SLSN
CLASSIFICATION_FILTER=

# Filter by keyword in comments (case-insensitive)
# Example: probably sn, sn
COMMENT_KEYWORD=

# Note: When both filters are set, sources matching EITHER filter are included (OR logic)

# Maximum number of retries for failed requests
MAX_RETRIES=3

# Dust map data directory
DUSTMAPS_DATA_DIR=/tmp
